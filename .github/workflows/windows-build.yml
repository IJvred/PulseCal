name: Windows Build

on:
  workflow_dispatch:
  push:
    tags: ["v*"]

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install deps
        run: npm install

      - name: Build frontend (Vite)
        run: npm run build

      - name: Verify dist + build.rs (hard fail if missing)
        run: |
          echo "==== repo root ===="; dir
          echo "==== src-tauri ===="; dir src-tauri
          echo "==== show build.rs ===="; type src-tauri\build.rs
          if (Test-Path dist) { echo "✅ dist/ exists" } else { echo "❌ dist/ missing"; exit 1 }

      - name: Build (Tauri)
        run: npx tauri build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-bundles
          path: |
            src-tauri/target/release/bundle/**/*.exe
            src-tauri/target/release/bundle/**/*.msi
            src-tauri/target/release/bundle/**/*.zip
